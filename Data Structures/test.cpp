#include <bits/stdc++.h>
using namespace std;

template <typename T>
template <typename A>

T myMax(T x, T y)
{
    return (x > y) ? x : y;
}

A mymin(A x, A y)
{
    return (x > y) ? x : y;
}

int main(void)
{
}